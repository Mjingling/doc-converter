# Changelog

本文件记录 DocMorph 的重要功能变更，格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本语义化遵循 [SemVer](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **PDF 拆分命名规范化**：输出文件改为「原文件名_页码范围.pdf」（如 `合同_2-4.pdf`），单页范围省略连字符（如 `报告_5.pdf`），替代原先固定的 `split_N.pdf` 前缀
- **拆分面板实时预览**：选择文件后，每行页范围右侧实时显示将生成的文件名，长文件名保留头尾、中间省略，悬停可查看完整名称
- **拆分面板操作优化**：「添加范围 / 每段 N 页 / 自动生成」工具组移至页范围行右侧，未选输出目录时拆分按钮可直接使用（默认输出到源文件所在目录），并记住上次手动选择的输出目录
- **输出目录模式**：压缩、加密/解密、旋转/页码、水印、提取/删除 5 个面板统一改为「选择输出目录 + 自动命名（原名_后缀.pdf）」，规避 macOS 保存对话框在中文文件名下「存储」按钮无法确认的问题；输出目录优先级：上次手动选择 → 设置默认目录 → 源文件所在目录
- **自定义标题栏窗口拖动**：补齐 `core:window:allow-start-dragging` 权限，标题栏空白处、红绿灯留白、应用名区域可按住拖动窗口位置，双击空白处最大化
- **侧边栏「更多工具」折叠**：低频工具（元数据、裁剪、书签、DOCX 提取、图片压缩、图片提取、去水印等）折叠进分组，激活时自动展开；导航图标按语义着色（PDF 工具红、文档转换蓝、工具类绿）
- **AI 摘要原文预览**：生成摘要前可展开查看提取的原文内容
- **PDF 语义对比重试**：模型预热失败时提供「重试模型加载」按钮
- **操作进度反馈**：压缩/图片压缩按钮处理中显示「压缩中…」状态
- **网页转 PDF URL 校验**：自动补全 `https://` 前缀并校验 URL 格式
- **AI 云端配置连接测试**：设置中新增「测试连接」按钮验证云端配置可用性

### 修复

- macOS 保存对话框（NSSavePanel）在中文文件名下「存储」按钮灰色无法确认的问题（通过输出目录模式规避）
- 自定义标题栏无法按住拖动窗口位置的问题（权限缺失导致 start_dragging 被静默拒绝）
- 网页转 PDF 拖拽提示文案 i18n 化（原先硬编码中文）
- 文件扩展名提取工具 `extOf`：无扩展名时返回空字符串，避免全名误判为扩展名

### 变更

- 拆分面板「拆分 PDF」按钮激活条件放宽：只要选择了文件即可使用，未选输出目录时输出到源文件所在目录
- 压缩/加密/旋转/水印/提取删除面板提示文案统一标注输出文件名规则（如「输出文件名：原名_compressed.pdf」）
